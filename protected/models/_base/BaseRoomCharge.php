<?php

/**
 * This is the model base class for the table "tbl_room_charge".
 * DO NOT MODIFY THIS FILE! It is automatically generated by giix.
 * If any changes are necessary, you must set or override the required
 * property or method in class "RoomCharge".
 *
 * Columns in table "tbl_room_charge" available as properties of the model,
 * followed by relations of table "tbl_room_charge" available as properties of the model.
 *
 * @property integer $id
 * @property integer $room_id
 * @property integer $price
 * @property string $description
 *
 * @property Room $room
 */
abstract class BaseRoomCharge extends GxActiveRecord {

	public static function model($className=__CLASS__) {
		return parent::model($className);
	}

	public function tableName() {
		return 'tbl_room_charge';
	}

	public static function label($n = 1) {
		return Yii::t('app', 'RoomCharge|RoomCharges', $n);
	}

	public static function representingColumn() {
		return 'description';
	}

	public function rules() {
		return array(
			array('room_id, price, description', 'required'),
			array('room_id, price', 'numerical', 'integerOnly'=>true),
			array('description', 'length', 'max'=>255),
			array('id, room_id, price, description', 'safe', 'on'=>'search'),
		);
	}

	public function relations() {
		return array(
			'room' => array(self::BELONGS_TO, 'Room', 'room_id'),
		);
	}

	public function pivotModels() {
		return array(
		);
	}

	/**
	 * Prepares create_user_id and update_user_id attributes before
	 saving.
	 */
	protected function beforeSave()
	{
	  if(null !== Yii::app()->user)
	    $id=Yii::app()->user->id;
	  else
	    $id=1;
	  if($this->isNewRecord)
	    $this->create_user_id=$id;
	  $this->update_user_id=$id;
	  return parent::beforeSave();
	}
	
	
	public function behaviors()
	{
	  return array(
	    'CTimestampBehavior' => array(
	      'class' => 'zii.behaviors.CTimestampBehavior',
	      'createAttribute' => 'create_time',
	      'updateAttribute' => 'update_time',
	      'setUpdateOnCreate' => true,
	    ),
	  );
	}
	
	public function attributeLabels() {
		return array(
			'id' => Yii::t('app', 'ID'),
			'room_id' => null,
			'price' => Yii::t('app', 'Price'),
			'description' => Yii::t('app', 'Description'),
			'room' => null,
		);
	}

	public function search() {
		$criteria = new CDbCriteria;

		$criteria->compare('id', $this->id);
		$criteria->compare('room_id', $this->room_id);
		$criteria->compare('price', $this->price);
		$criteria->compare('description', $this->description, true);

		return new CActiveDataProvider($this, array(
			'criteria' => $criteria,
		));
	}
}